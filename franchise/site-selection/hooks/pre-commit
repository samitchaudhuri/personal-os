#!/usr/bin/env bash
# Back-compat: older clones symlink .git/hooks/pre-commit here.
# Canonical hook is hooks/pre-commit at the repo root.
set -euo pipefail
REPO_ROOT="$(git rev-parse --show-toplevel)"
exec "$REPO_ROOT/hooks/pre-commit"
